services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: astor
      POSTGRES_PASSWORD: astor
      POSTGRES_DB: astor
    ports:
      - "5432:5432"
    volumes:
      - astor_pgdata:/var/lib/postgresql/data

volumes:
  astor_pgdata:
